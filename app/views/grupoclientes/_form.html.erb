<%= form_for @grupocliente, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :descricao, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :descricao, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :racio, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :racio, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                grupoclientes_path, :class => 'btn' %>
  </div>
<% end %>
