<%- model_class = Profissao -%>
 <h4><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h4>
 
<table class="table table-striped">
  <thead>
    <tr>
      <th>#</th>
      <th><%= model_class.human_attribute_name(:descricao) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @profissaos.each do |profissao| %>
      <tr>
        <td><%= link_to '<i class="icon-file"></i>'.html_safe, profissao_path(profissao) %></td>
        <td><%= profissao.descricao %></td>
        <td>
          <%= link_to '<i class="icon-pencil"></i>'.html_safe,
                      edit_profissao_path(profissao), :class => 'btn btn-mini' %>
          <%= link_to  '<i class="icon-remove"></i>'.html_safe,
                      profissao_path(profissao),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_profissao_path,
            :class => 'btn btn-primary' %>
