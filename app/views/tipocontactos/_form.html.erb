<%= form_for(@tipocontacto) do |f| %>
  <% if @tipocontacto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipocontacto.errors.count, "error") %> prohibited this tipocontacto from being saved:</h2>

      <ul>
      <% @tipocontacto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
